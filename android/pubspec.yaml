name: mclash
version: 2.2.0+2
publish_to: none

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^8.3.1
  url_launcher: ^6.3.2
  # Newer releases require Android Gradle Plugin 8.9.1 or later.
  url_launcher_android: 6.3.20

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
